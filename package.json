{
  "name": "getJiraIssue",
  "displayName": "getJiraIssue",
  "description": "This is a demo extension for Visual Studio Code.",
  "version": "1.0.2",
  "publisher": "mySinghtest1",
  "main": "./out/extension.js",
  "engines": {
    "vscode": "^1.50.0"
  },
  "activationEvents": [
    "onCommand:extension.getJiraIssue"
  ],
  "contributes": {
    "commands": [
      {
        "command": "extension.getJiraIssue",
        "title": "Hello World"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./"
  },
  "devDependencies": {
    "typescript": "^4.0.0",
    "vscode": "^1.1.37"
  },
  "private": true
}
